Documentation technique et notes d'implémentation.

Voir `CDC/` pour les spécifications métier et techniques.

Utilitaire frontend:
- `window.fillFromPatient(patient)` : fonction exposée en console dans l'UI d'admission permettant de pré-remplir rapidement le formulaire à partir d'un objet `Patient` FHIR. Appel utile pour tests manuels et démo.
