{
  "name": "sujet-a-admission",
  "version": "0.1.0",
  "description": "Skeleton Node.js + frontend for admission pipeline (FHIR -> HL7)",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest --coverage",
    "test:integration": "jest --config=jest.integration.config.js",
    "test:e2e": "echo \"Run E2E with cypress or puppeteer\"",
    "test:features": "cucumber-js tests/features --require tests/step_definitions"
  },
  "dependencies": {
    "axios": "^1.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "nock": "^13.2.0",
    "cucumber": "^6.0.5",
    "cucumber-js": "^7.0.0"
  }
}
